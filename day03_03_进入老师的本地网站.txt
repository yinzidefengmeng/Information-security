192.168.0.112:99  本地连接  端口99


Freebuf

账号泄露防护测试
			A类一套   账号密码
			B类一套		账号密码
			C类一套		账号密码
			

指令集
		X64  理论上可以处理2^64位的数据
					
		目前性价比最高CPU(amd的)  ryzenCPU
					
		MMX 	多媒体扩展指令集  包含57条指令
		SSE		单指令多数据流扩展指令集 	包含70条指令
		SIMD 	单指令多数据
								渗透测试没有用，用不到上面的指令集，理由：到不了这么深
		软件的线程数和CPU的线程数要匹配，不然会浪费时间

		液晶屏(面板)  生产商： 三星(最黑，自己生产却不用在自己的电脑上)   lg  夏普(最贵)


		栈  先进后出 物理位置
		堆 	

		微软的com技术   
		不给接口，非用不可的状态  暴力调用(从堆地址往下)
		
语言分类：直接编译成2进制代码  c  c++ delphi 汇编语言
					编译成中间文件(都可被反编译成源代码)    java 安卓  go语言(不可被反编译)  vb python  c#
反编译  反汇编
					
					
硬件
			主机
							中央处理器
							内存储器
			
			外设
							外存储器
							输入设备
							输出设备
							其他  （显卡   amd 英美达  生产商：三星  台积电
